import React from "react";

const RevealText = ({
  field,
  id,
  align = "start",
  as = "div",
  duration = 0.8,
  className,
  staggerAmount = 0.1,
}) => {
  return <div>RevealText</div>;
};

export default RevealText;
